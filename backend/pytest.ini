# Pytest configuration for the backend.
#   cd backend && python -m pytest
[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    # Our code is clean; silence noise from dependencies (Starlette/anyio/pydantic).
    ignore::DeprecationWarning
