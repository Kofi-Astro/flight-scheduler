# The frontend server uses only the Python standard library — no pip install.
# This tells Nixpacks to just provide Python and run the server.
[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["echo 'no dependencies to install'"]

[start]
cmd = "python server.py"
